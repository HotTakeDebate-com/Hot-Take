PORT=3001

# Set to 0 only if Node listens on the public internet without a trusted reverse proxy (disables trusting X-Forwarded-For).
# Default: trust first proxy hop so rate limits use the real client IP on Railway, Render, Fly, etc.
# TRUST_PROXY=0

# Matchmaking abuse (server): max join-queue events per IP per window (in-memory; use a shared store if you scale horizontally).
# RATE_LIMIT_JOIN_QUEUE_MAX=40
# RATE_LIMIT_JOIN_QUEUE_WINDOW_MS=60000

# Optional: JSON array of ICE servers (add TURN for strict NAT / symmetric NAT).
# Example TURN (replace with your provider):
# ICE_SERVERS_JSON=[{"urls":"stun:stun.l.google.com:19302"},{"urls":"turn:global.turn.example.com:3478","username":"user","credential":"pass"}]
# Recommended: Cloudflare Realtime TURN. The server exchanges these secrets for
# short-lived browser credentials and never exposes the API token to clients.
# CLOUDFLARE_TURN_KEY_ID=
# CLOUDFLARE_TURN_KEY_API_TOKEN=
# Optional: custom lobby stale timeout in ms (default 1800000 = 30 minutes)
# CUSTOM_LOBBY_TTL_MS=1800000
# Optional: server metrics log interval in ms (default 300000 = 5 minutes)
# METRICS_LOG_EVERY_MS=300000

# Optional: in-debate text chat limits (server)
# DEBATE_CHAT_MAX_LEN=2000
# DEBATE_CHAT_MAX_PER_MIN=30

# --- Optional Redis (multi-instance / viral traffic) ---
# When set, enables @socket.io/redis-adapter (broadcasts + rooms across Node processes) and
# shared per-IP join-queue rate limits. Use Railway/Upstash Redis or any redis:// URL.
# REDIS_URL=

# --- Socket.IO auth hardening (server runtime) ---
# To enforce Firebase ID token verification on Socket.IO:
# 1) provide Firebase Admin credentials via one of:
#    - FIREBASE_ADMIN_SERVICE_ACCOUNT (JSON string OR base64-encoded JSON), OR
#    - GOOGLE_APPLICATION_CREDENTIALS (path to service-account JSON on host)
# 2) set REQUIRE_FIREBASE_TOKEN=true
# If REQUIRE_FIREBASE_TOKEN is false/missing, server allows local dev fallback.
# When Admin is initialized, the server persists users/{email}/debates/{roomId} + chat_messages (Admin SDK).
# REQUIRE_FIREBASE_TOKEN=true
# FIREBASE_ADMIN_SERVICE_ACCOUNT=

# --- Operator moderation API (server only) ---
# Long random secret. Send as header X-Hot-Take-Moderation: <value> or Authorization: Bearer <value>.
# Legacy: X-Chitchat-Moderation and CHITCHAT_MODERATION_SECRET still work if set.
# Endpoints: GET/POST https://<host>/api/mod/... — see docs/MODERATION.md
# HOT_TAKE_MODERATION_SECRET=
# CHITCHAT_MODERATION_SECRET=

# --- Pre-signup email deliverability screening (server runtime) ---
# Create a private API key at https://emailable.com and store it only in Railway.
# Email/password signup fails closed when this key is missing or the provider is unavailable.
# EMAILABLE_API_KEY=
# Optional per-IP limit for validation attempts (defaults: 10 checks per 60 seconds).
# EMAIL_VALIDATION_MAX=10
# EMAIL_VALIDATION_WINDOW_MS=60000

# --- Report confirmation emails (server runtime) ---
# Resend API key and verified sender used to confirm that a safety report was received.
# These values belong in Railway only; never expose them with a VITE_ prefix.
# RESEND_API_KEY=
# REPORT_EMAIL_FROM=Hot Take Support <support@hottakedebate.com>
# REPORT_EMAIL_REPLY_TO=support@hottakedebate.com

# --- Firebase (client / Vite — must use VITE_ prefix) ---
# Create a Web app in Firebase Console → Project settings → Your apps.
# Enable: Authentication → Sign-in method → Email/Password (required for Hot Take).
# The app does not use Firebase Anonymous sign-in; use email/password accounts only.
# Firestore: create database (test mode ok for first try; then use firestore.rules in repo).
#
# Verification email still says "Chit Chat"? Update Firebase Console (not .env):
#   1) Project settings → General → Public-facing name → "Hot Take" (powers %APP_NAME% in emails)
#   2) Authentication → Templates → Email address verification (and Password reset) → replace Chit Chat with Hot Take
#   3) Authentication → Settings → Authorized domains → add your production host (and localhost for dev)
#   4) Set VITE_APP_URL below so verification links return to your live site (also add that domain in step 3)
# VITE_APP_URL=https://your-production-domain.com
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# Optional — enables Firebase Analytics (same as measurementId in Console config)
VITE_FIREBASE_MEASUREMENT_ID=

# Public contact email — Support page, Privacy Policy, Recording Agreement (mailto when set)
VITE_CONTACT_EMAIL=support@hottakedebate.com
